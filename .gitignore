# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Environment variables (contains secrets)
.env
.env.local
.env.production
.env.development
.env.*

# User data and sensitive files
backend/users.json
backend/*.log

# VPN configuration files (NEVER commit these!)
*.ovpn
*.ovpn.*
backend/*.ovpn
backend/*.ovpn.*
**/*.ovpn
**/*.ovpn.*

# SSH keys and certificates (NEVER commit these!)
*.pem
*.key
*.crt
*.cert
*.p12
*.pfx
id_rsa*
id_ed25519*
id_ecdsa*

# Credential files (NEVER commit these!)
*credential*
*password*
*secret*
*auth.txt
*auth.json
*auth.key
*auth.pem
*auth.crt
vpn-credentials.txt
backend/vpn-credentials.txt

# Database files with potentially sensitive data
*.db
*.sqlite
*.sqlite3

# Backup files that might contain sensitive data
*.bak
*.backup
*.old
*.orig

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# macOS
.DS_Store
.AppleDouble
.LSOverride

# Windows
Thumbs.db
ehthumbs.db
Desktop.ini
